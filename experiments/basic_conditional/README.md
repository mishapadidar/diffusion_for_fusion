
Train a conditional diffusion model on the QUASR data.

`train_model.py` is the my script which trains the diffusion model. 
Launch the training by running `./train.sh`.

`load_quasr_data.py` has convenience function for loading and standardizing the data.