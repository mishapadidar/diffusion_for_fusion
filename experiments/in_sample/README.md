# In-sample evaluation

In this dir, we evaluate the model in-sample and make plots.

- `evaluate_model.py` evaluates the model and saves the evaluation data.
- `evaluate_baseline.py` evaluates data from QUASR to create a baseline.