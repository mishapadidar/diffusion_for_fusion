# Stellarator Example
Make figure 1.

- run `./load_config.py` to generate the stellarator paraview files + the field line
- load the paraview states, `symmetries_full.pvsm, symmetries.pvsm` to generate the two panels of the stellarator example figure
